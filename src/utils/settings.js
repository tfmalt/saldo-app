const settings = {
  urls: {
    token: 'https://api.malt.no/banking/oauth2/token',
    balance: 'https://api.malt.no/banking/balance',
  },
}

export default settings
export const urls = settings.urls
